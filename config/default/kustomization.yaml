apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: karpetrack-system

resources:
  - namespace.yaml
  - ../crd
  - ../rbac
  - ../manager

images:
  - name: controller
    newName: ghcr.io/karpetrack/karpetrack
    newTag: latest
